
//Action Types are Objects
// Key-value pairs that helps the reducer connect the 
// payload data with which part of the state to update

export const ActionTypes = {  
    SET_USERS: "app/containers/HomePage/SET_USERS"  // we do it this way to make it unique
}